/**
 * Look at the function below, can you think of a simple way to
 * make this function more efficient?
 */
function print_even_numbers() {
  let number = 2;

  while (number <= 100) {
    console.log(number);

    number += 2;
  }
}

// print_even_numbers();
